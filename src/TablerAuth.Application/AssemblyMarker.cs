namespace TablerAuth.Application;

/// <summary>
/// Application layer marker. Use cases, DTOs, and service interfaces live here (login, tokens). Controllers call this layer; they do not talk to SQL.
/// </summary>
public static class AssemblyMarker
{
}
