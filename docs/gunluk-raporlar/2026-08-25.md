# 2026-08-25 — Faz 1–2, Identity, JWT

## Ne yapmayı planladın?

- 8 fazlı öğrenme planını projeye koymak (`docs/PLAN.md`) ve günlük rapor şablonunu eklemek.
- Wget ile inen Tabler sitesi aynasını temizlemek.
- .NET 8 katmanlı MVC çözümünü oluşturup `dotnet run` ile ayağa kaldırmak.
- Connection string'i User Secrets'a almak; `.cursorrules` ve `.gitignore` yazmak.
- Bu klasörde git açmak ve GitHub remote bağlamak (commit yok).
- Faz 2: kökteki Tabler Login/Register/_Layout taslaklarını `src/TablerAuth.Web` içine taşımak; formlar henüz kayıt yapmasın.
- Faz 4: kısa ömürlü access JWT + DB'de rotate edilen refresh token; API login/refresh; korumalı `/api/me`. MVC sayfaları cookie'de kalsın.

## Hangi sorunlar ile karşılaştın?

- Workspace bir ASP.NET projesi değil, `tabler.io` wget kopyasıydı. Kökte `docs` adında bir HTML dosyası vardı; `docs/` klasörü açılamıyordu.
- `dotnet` PATH'te yoktu; SDK yüklü değildi.
- `git` kökü ev diziniydi (`/Users/hamitsulucan`); bu klasör ayrı repo değildi.
- `gh` CLI yüklü değildi.
- Taslaklar bir ara hem kökte hem Web'de duruyordu; iki kopya ileride kayardı.
- Varsayılan `_Layout.cshtml.css` Tabler buton/link renklerini ezecekti.
- `dotnet run --no-build` yeni `AddRefreshTokensTable` migration'ını derlemeden açınca tablo oluşmadı; yeniden build gerekti.

## Bu sorunları nasıl çözdün?

- Wget artığını sildik; sonra `docs/PLAN.md` yazılabildi.
- Homebrew ile `dotnet@8` kuruldu; `TablerAuth.sln` ve dört katman derlendi. `http://localhost:5059/` HTTP 200 döndü.
- `tabler.io` içinde `git init` yapıldı. GitHub private repo GitHub MCP ile oluşturuldu; `origin` eklendi. İlk commit bilinçli olarak atılmadı.
- Connection string ve JWT imza anahtarı `appsettings.json`'a yazılmadı; User Secrets'ta.
- Taslaklar Web'e alındı; kök `Views/` ve `wwwroot/` silindi. Login/Register `_AuthLayout` kullanıyor.
- JWT için `TokenService` + `RefreshTokens` tablosu eklendi. Access 15 dk; refresh hash'lenerek saklanıyor, kullanınca yenileniyor. `/api/me` tokensız 401, tokenla 200. MVC login cookie olarak duruyor.
